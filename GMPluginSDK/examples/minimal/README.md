# Minimal lifecycle

The smallest plugin: validate ABI tables, save the Host pointer and register a
single `on_load` callback. Start here when creating a new plugin.

After changing this example, run `./gm-build` from the SDK root.
