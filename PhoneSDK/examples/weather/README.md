# App built-in weather plugin

Source workspace for the App built-in plugin `com.memomind.demo.weather`.

Run it from the `PhoneSDK` workspace root:

```sh
node tools/run-browser-studio.mjs --plugin examples/weather
```

Package it with:

```sh
npm run pack:plugin -- examples/weather dist/weather.mmpkg
```
